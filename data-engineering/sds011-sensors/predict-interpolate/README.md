# Time Series Prediction Module

This job runs the time series prediction and saves the result in the /data/parquet directory.

To get going here:
1. Build the docker image.
2. Push the docker image.
3. Apply the kubernetes file.

Happens after aggregate and before front end.

